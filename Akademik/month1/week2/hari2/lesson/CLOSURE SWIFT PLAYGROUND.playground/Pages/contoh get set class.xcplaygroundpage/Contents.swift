//: [Previous](@previous)

import Foundation
/*
class Rectangle {
    // Stored property
    private var _width: Double = 0.0
    private var _height: Double = 0.0
    
    // Computed property for area
    var area: Double {
        return width * height
    }
    
    // Computed property with getter and setter
    var width: Double {
        get {
            return _width
        }
        set {
            print("Width akan diubah dari \(_width) menjadi \(newValue)")
            _width = newValue
            print("Width telah diubah menjadi \(_width)")
        }
    }
    
    var height: Double {
        get {
            return _height
        }
        set {
            print("Height akan diubah dari \(_height) menjadi \(newValue)")
            _height = newValue
            print("Height telah diubah menjadi \(_height)")
        }
    }
}

let rect = Rectangle()
rect.width = 5.0
rect.height = 10.0
print("Area dari rectangle: \(rect.area)")

// Output:
// Width akan diubah dari 0.0 menjadi 5.0
// Width telah diubah menjadi 5.0
// Height akan diubah dari 0.0 menjadi 10.0
// Height telah diubah menjadi 10.0
// Area dari rectangle: 50.0

*/
